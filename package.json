{
  "name": "boardline",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/sync-public-data.mjs && vite",
    "build": "vue-tsc -b && node scripts/sync-public-data.mjs && vite build",
    "preview": "vite preview",
    "collect": "node scripts/collect-data.mjs"
  },
  "dependencies": {
    "vue": "^3.5.21"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@vitejs/plugin-vue": "^6.0.1",
    "typescript": "~5.9.2",
    "vite": "^7.1.4",
    "vue-tsc": "^3.0.6"
  }
}
